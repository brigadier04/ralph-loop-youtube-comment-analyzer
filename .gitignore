# Ralph runtime artifacts
scripts/ralph/.last-branch
scripts/ralph/progress.txt
scripts/ralph/archive/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Local env / secrets (BYOK keys live in browser localStorage, not on disk)
.env
.env.local
.env.*.local

# Dependencies (project intentionally has no bundler / node_modules)
node_modules/

# Build artifacts (project is static; no build step expected)
dist/
build/

# Logs
*.log
npm-debug.log*
